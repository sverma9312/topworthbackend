server.port=8080
server.servlet.context-path=/topWorth

## default connection pool
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

## PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/topworth
spring.datasource.username=postgres
spring.datasource.password=postgres

#drop n create table again, good for testing, comment this in production
spring.jpa.hibernate.ddl-auto=create

#jpa dialect
spring.jpa.properties.hibernate.jdbc.dialect: org.hibernate.dialect.PostgreSQLDialect

forbs400.url=https://forbes400.herokuapp.com/api/forbes400
forbs400.limit=10
